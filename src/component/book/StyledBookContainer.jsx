import { styled } from '@mui/system';
import { Box } from '@mui/material';

const StyledBookContainer = styled(Box)({
    
    display: 'flex',
    justifyContent: 'center',
    flexDirection: 'row',

});

export default StyledBookContainer;
