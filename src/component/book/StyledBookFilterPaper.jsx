import { styled } from '@mui/system';
import { Paper } from '@mui/material';

const StyledBookFilterPaper = styled(Paper)({
    
    width: '100%',
    height: '100%',
    textAlign: 'center'

});

export default StyledBookFilterPaper;