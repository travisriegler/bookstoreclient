import { styled } from '@mui/system';
import { Avatar } from '@mui/material';

const StyledBookImage = styled(Avatar)({
    
    width: '180px',
    height: '200px',
    
});

export default StyledBookImage;