import { styled } from '@mui/system';
import { Paper } from '@mui/material';

const StyledBookListItemPaper = styled(Paper)({
    
    padding: '10px',
    display: 'flex',
    width: "80%"

});

export default StyledBookListItemPaper;